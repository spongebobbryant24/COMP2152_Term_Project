Video Script 3 — Insecure Service Port Exposure

Hello, my name is [Your Name].

The vulnerability I found is insecure service port exposure on [target subdomain].

This is a security risk because unnecessary or insecure services such as Telnet, FTP, SMTP, or Redis can expose sensitive data or expand the attack surface.

Now I will run my Python script. The script checks a small set of ports mentioned in the assignment and reports whether any risky service appears open.

[Run insecure_port_check.py and show output]

As shown in the terminal, the script reports which ports are open and explains why that matters.

In the code, I validate that the target is in scope, then use Python sockets to connect to each allowed test port one at a time. I also added a delay to avoid hitting the rate limit.

On GitHub, this work was done on my branch named [yourname_insecure_service], and then merged into master.

Thank you.
