Video Script 1 — Insecure HTTP

Hello, my name is [Your Name].

The vulnerability I found is insecure HTTP on [target subdomain].

This is a security risk because if a site does not force HTTPS, traffic can be exposed in cleartext and intercepted.

Now I will run my Python script. The script connects to the target over HTTP, checks the final URL, and reports whether the site stays on HTTP instead of redirecting securely to HTTPS.

[Run insecure_http_check.py and show output]

As shown in the terminal, the script reports the finding and explains why it matters.

In the code, I define the target, verify that it is in scope under 0x10.cloud, then use urllib to open the page and inspect the final URL. I also included try-except blocks for safe error handling.

On GitHub, this work was done on my branch named [yourname_insecure_http], and then merged into master.

Thank you.
